Tighten the Integrations detail Drawer UI to feel like Apple settings sheets:
- compact headings
- consistent bullet counts
- clear “Setup requirements” mini block
- less vertical padding
- more structure, less prose

DO NOT change:
- Integrations grid cards
- filters/search UI
- page layout sections

ONLY update the Drawer component and styling used when opening an integration.

============================================================
DRAWER LAYOUT (LOCKED STRUCTURE)
Drawer content order:

1) Header
2) Section: What we ingest
3) Section: What it unlocks
4) Section: Setup requirements
5) Footer actions

No additional sections.

============================================================
1) HEADER (COMPACT)
- Integration name (text-lg md:text-xl font-semibold)
- Status badge small
- One-line description (text-sm text-muted), max-w ~60ch
- Reduce header padding (p-5 md:p-6)

============================================================
2) SECTION STYLES (APPLE SETTINGS FEEL)
Each section:
- Title: text-xs uppercase tracking-widest text-muted
- Content container: mt-3
- Use subtle dividers between sections: border-white/10 (dark theme)
- Keep section padding tight: py-5 (not py-8/10)

============================================================
3) CONSISTENT BULLET COUNTS (IMPORTANT)
Enforce bullet list counts:
- What we ingest: show MAX 5 bullets
- What it unlocks: show MAX 5 bullets

If data contains more than 5 items:
- show first 5 only
- do NOT add “and more”
- do NOT scroll inside lists

Bullet styling:
- text-sm
- leading-6
- space-y-2
- use a subtle dot marker (•) or tiny neutral marker
- avoid colored icons

============================================================
4) SETUP REQUIREMENTS MINI BLOCK (NEW)
Create a compact “Setup requirements” block styled like a settings card:

- Container: rounded-xl border border-white/10 bg-white/5
- Padding: p-4 md:p-5
- Use a 2-column grid on desktop, 1 column on mobile

Fields (always present):
- Method
- Time to value
- Permissions

Optional fields (only if present in data):
- Data location (e.g. “Billing export bucket / BigQuery dataset”)
- Notes (1 line max)

Field styling:
- Label: text-xs uppercase tracking-widest text-muted
- Value: text-sm font-medium text-foreground
- Tight gaps: gap-4

============================================================
5) FOOTER ACTIONS (CLEAN)
Footer:
- Keep 2 buttons max
Primary: Request access / Talk to us (keep current logic)
Secondary: View setup docs (ONLY if link exists; otherwise omit)
- Tight footer padding
- Add a subtle top border divider

============================================================
VISUAL CLEANUP (CRITICAL)
- Remove any large whitespace: reduce mt/mb across Drawer
- Avoid multiple nested cards
- Avoid shadows and gradients
- Do not change theme colors
- Ensure Drawer width is comfortable: ~420–520px on desktop (if adjustable)

============================================================
DELIVERABLE
- Drawer reads like an Apple settings sheet
- Headings compact and consistent
- Lists capped to 5 items each
- Setup requirements presented as a clean mini block
- No layout changes to the Integrations page itself
